package tn.eniso.main.service;

import net.vpc.upa.config.*;
import java.util.logging.Logger;

/**
 * Application UPA interceptor for Main module.
 * 
 * @author Houssem
 */
@Callback
public class TestMainInterceptor {
    private static final Logger log = Logger.getLogger(TestMainInterceptor.class.getName());
}